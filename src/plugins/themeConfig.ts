const themeConfig = {
    token: {
        colorPrimary: "#1e88e5", // Màu chính (BLUE_THEME)
        borderRadius: 12, // Bo góc giống Vuetify
    },
    components: {
        Button: {
            borderRadius: 8,
        },
        Card: {
            borderRadius: 12,
        },
        Input: {
            borderRadius: 8,
        },
    },
};

export default themeConfig;
